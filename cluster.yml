---
- name: Common roles
  hosts: tag_acl-axiom
  gather_facts: true
  remote_user: root
  vars:
    k3s_version: v1.28.2+k3s1
  roles:
    - role: prepos
    - role: download

- name: Control Plane
  hosts: tag_acl-control-plane
  remote_user: root
  # roles:
  # - role: k3s/master

- name: Compute Plane
  hosts: tag_acl-compute-plane
  remote_user: root
  # roles:
  # - role: k3s/worker
